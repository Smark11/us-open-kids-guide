# qa-engineer review — iteration 2
Verdict: PASS

Evidence: Playwright (Chromium headless-shell 1228), viewports 390×844 and 768×1024, `file://` and `http://localhost:8765/us-open-kids-guide/` (subpath). Scripts + logs in scratchpad: `qa.js`/`qa2.js` (`qa-iter2.log`, `qa-iter2-part2.log`), `sw-probe2.js` (`sw-probe2-iter2.log`), `probe-iter2.js`, `probe-focus.js` (`probe-focus.log`); screenshots `review-shots/qa-engineer/it2-*.png` plus refreshed `*-top/grid.png`. Commit under test: `92cfc8d` + re-merged data (`generatedAt 2026-09-04T22:52:55`).

## Iteration-1 items
- [B1] SW precache — **FIXED.** `data/players.js` now emits `self.PLAYERS_DATA`; `VERSION = 'nora-usopen-v2'`. Evaluated *inside the worker*: `typeof window === 'undefined'`, `importScripts` succeeds (`players=32`), `photoList().length === 32`, `caches.open('nora-usopen-v2').keys()` → **39 entries = 7 shell + 32 `img/players/*`**, all under `/us-open-kids-guide/`, zero photos in data missing from the cache; old `nora-usopen-v1` deleted on activate. Cold-offline test (fresh context, one online load with no scrolling → offline → `#p/coco-gauff`): sheet opens with photo, grid shows 33/33 images, 0 avatar fallbacks (`it2-http-phone-cold-offline-detail.png`, `it2-http-phone-cold-offline-grid.png`).
- [B2] Deep-link close leaves site — **FIXED.** `openedFromPage` is only set by the in-page click handler; `requestClose()` uses `replaceState` + `closeDetail()` otherwise. With prior history (`localhost:8765/` → `…/#p/coco-gauff`): ✕, Escape, and the nav "✕ Close" all stay at `…/us-open-kids-guide/` with the sheet hidden and body unlocked; deep link → next → Escape stays; in-page open → ✕/Escape stays with hash cleared; open → browser back → forward → ✕ stays. Fresh-tab deep link + ✕ also stays.
- [S1] Focus ring on cream — **FIXED.** New rule `.pill:focus-visible, .card:focus-visible, .fav:focus-visible, .d-close:focus-visible, .flip:focus-visible, .match-player:focus-visible, .opp:focus-visible, summary:focus-visible` at the end of the sheet. Keyboard-driven focus (Tab/Shift+Tab) shows `outline 4px #E0FF3A` + `box-shadow 0 0 0 7px #0B1B4A` on: filter pills, cards, stars, match rows, credits summary, `#clear-filters` in the cream empty box, `#d-close`, dialog star, flip cards, "Where this came from" summary, source links, prev/next and Close pills in `.d-nav` (`it2-focus-navpill.png`, `it2-focus-clearfilters.png`). Ring-vs-surround contrast: ink halo on cream 15.8:1, yellow on blue 5.5:1 — all ≥3:1. One residual: `.box.match-box .opp` (specificity 0,3,0) still overrides the halo, but the yellow outline on the blue match box is 5.47:1, so it passes (see N1).
- [S2] Tap targets in sheet — **FIXED.** At 390: `.opp` 217×50, `details summary` 338×48 (dialog one ≥44), `.src-list a` 274×45, `[data-nav]` 128×55, `[data-close]` 93×55, `#d-close` 60×60, dialog star 60×60, flips 172×160. Only `.verified a` "source" is 50×43 (1px short; see N2).
- [N1] Tab order to cards — **FIXED** by moving the album above the matches while OOP is unpublished: skip link → jump pill → 7 filter pills → first card = 9 presses (was ≈34).
- [N2] Flip faces in a11y tree — **FIXED.** `.back` starts `aria-hidden="true"`; clicking sets front `true` / back `false` and back again on unflip.
- [N3] Fixed ✕ covering sheet text — **FIXED.** `#d-close` is now `position: absolute` inside `.d-top`; after scrolling the sheet 900px it is off-screen (top −888), nothing is covered; the sticky `.d-nav` "✕ Close" remains reachable (`it2-sheet-scrolled.png`).
- [N4] factCheck in players.js — **FIXED.** Key dropped in `build_data_js.py`; `players.js` 103.1KB raw / 23.3KB gzip (was 131.7KB / 33.5KB). Embedded object equals `players.json` minus `factCheck` byte-for-byte.
- [N5] Blank `#count` on empty result — **FIXED.** ⭐ filter with no favorites now reads "0 of 32 players"; Women + Underdogs = "4 of 32 players" (there are now tier-3 women since tiers became per-player).

## Blocking (must fix)
- none

## Should fix
- [S1] Perf regression from the layout fix: first load at 390 (no scroll, http) is now **25 requests / 910KB** (was 10 / 274KB): with the grid above the matches, Chrome's lazy-load threshold eagerly fetches the first **16 player JPEGs (664KB)**. Shell itself is lean (index 3.9KB, css 21.3KB→5.0KB gz, app.js 24.8KB→7.6KB gz, players.js 103KB→23KB gz, fonts 86.7KB). Options: ship a ~360px-wide grid thumbnail per player (cards render 171px CSS wide at 390) and keep the 600px file for the sheet, or re-encode the JPEGs to q≈70 (targets ≤25KB each). After the SW installs everything is cached, so this only hits the very first open.

## Nice to have
- [N1] `styles/main.css`: `.box.match-box .opp { … box-shadow: var(--shadow-sm) }` beats `.opp:focus-visible` (0,2,0 vs 0,3,0), so the opponent button gets only the yellow outline (still 5.47:1 on the blue box). Change the focus selector to `.box.match-box .opp:focus-visible` or add `!important` on the focus box-shadow (`it2-focus-opp.png`).
- [N2] `.verified a` "source" link in the sheet is 50×43 (padding 10px + 23px line); `.credits a` license links in the footer are 111×23. Bump `.verified a` padding to 11px and give `.credits li` / `.credits a` a 44px min-height.
- [N3] Match cards under a tier filter show partial matches (e.g. 🌱 Seeded → 11 matches, some with one seeded and one unseeded player). That's arguably right (the kid wants to know where her seeded player plays), just noting it is by design.

## What's great (keep)
- HTML validity: `html-validate 11.13.0` exit 0; python tag-balance parser: 0 unclosed/mismatched. All 35 JSON files parse; 32 players / 16 matches, no duplicate slugs, every match slug has a card and vice versa; 32 per-player files == merged set; 32/32 `verified`.
- Images: all 32 `photo.localPath` files exist; largest `nikola-bartunkova.jpg` 73.5KB; all ≤150KB, ≤598px wide; album total 1.25MB. After a slow scroll every `<img>` is `complete` with `naturalWidth>0` (32/32) under `file://` and `http` at 390 and 768; 0 missing `alt`; 0 failed requests. Blockx SVG avatar renders in the sheet (`http-tablet-detail-blockx.png`); forced 404 on a card image swaps in `svg.avatar` with `aria-label`.
- Console: 0 errors/warnings on `file://` and the subpath at both viewports, in the SW context, and during the interaction suite (the single 404 is my deliberate `does-not-exist.jpg`). No absolute `/` paths in `index.html`, `app.js`, `sw.js`, `main.css`, `manifest.webmanifest` (`start_url`/`scope` = `./`); SW registers with scope `…/us-open-kids-guide/` and state `activated`.
- Interaction: all 32 cards open (name, `#p/<slug>`, photo loaded, 6–8 fun facts, opponent button, sources, focus → ✕) and close (hash cleared, focus returns to the card) under both origins. Deep link `#p/coco-gauff` opens on load, body locked. Back button, Escape, Escape-after-prev/next all close correctly; prev/next + ←/→ work and wrap (first → Zheng Qinwen). Opponent button opens the opponent. Favorites persist across reload (`localStorage nora-favorites`, `aria-pressed`, ⭐, label "Remove Coco Gauff from favorites", ⭐ dot on the match row). Every pill yields a non-empty set (Women 16, Men 16, Superstars 8, Seeded 14, Underdogs 10) and filters the match list too; ⭐ with no favorites shows the labelled empty box, "Show everyone" resets. Flip cards toggle via click, Enter, Space.
- Accessibility: Tab reaches skip → jump → pills → cards/stars; inside the dialog ✕ → star → opponent → flips → summary → nav with the Tab trap wrapping (Shift+Tab from ✕ lands on "next"). Contrast: 80 unique text styles sampled at 390 (page, open sheet, active Seeded + ⭐ pills): every real text ≥5.47:1 (lowest: yellow "Underdogs" in the filter note 5.47:1, purple "seed" 5.53:1, white on purple flip face 5.81:1). The single sub-4.5 reading is the sampler's known false positive on the gradient seed badge (real ≈14:1). Reduced motion: ball/pill/star animations `none`, transitions `0s`, sheet rise `none`, `scroll-behavior auto`, flip is an instant display swap.
- Tap targets on the page: pills 48px tall (105–176px wide at both viewports), stars 48×48, cards 176×327, match rows ≥146×72, skip link 218×58.
