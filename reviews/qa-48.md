# qa-engineer review — 48-player build (Sat 32 + Sun 16)
Verdict: PASS

Evidence: Playwright (Chromium headless-shell 1228), viewports 390×844 (touch/mobile) and 768×1024, `file:///…/us-open-kids-guide/index.html` and `http://localhost:8765/us-open-kids-guide/` (subpath). Commit under test `2adefaa` (data `generatedAt 2026-09-05T14:07:11`, schedule `fetchedAt 2026-09-05T17:40:50Z`). Scripts + logs in scratchpad: `qa48.js` → `qa48.log` (4 origin/viewport runs), `qa48b.js` → `qa48b.log` (fresh-tab deep link, m14, Blockx, touch taps), `sw48.js` → `sw48.log`. Screenshots `review-shots/qa-48/{file,http}-{phone,tablet}-{grid-today,grid-sunday,detail-sunday-alcaraz,detail-done-potapova,detail-live-fritz,matches-top,matches-done,matches-sunday}.png` (32), all viewed.

## Checklist results (all 4 origin × viewport runs unless noted)
- **Console / network:** 0 errors, 0 warnings, 0 failed/4xx requests on file:// and subpath at both viewports, including the SW registration run and the whole interaction suite.
- **Day toggle:** three pills `🎾 Playing today (Sat)` (default, `aria-pressed=true`) / `📅 Playing Sunday` / `All` → **32 / 16 / 48** cards. Count line reads "32 today · 16 more still in it" / "16 playing Sunday" / "All 48 players". 0 `📅 Sun` chips on the Sat grid, 16 on the Sunday grid.
- **All 48 cards open and close (loop, per run):** each opens with `#p/<slug>`, body locked, focus on ✕, photo loaded (`naturalWidth>0`) with alt, ≥5 fun facts; ✕ closes, hash cleared, focus returns to the card. 0 problems × 4 runs. Touch `tap()` on pills, a card and "✕ Close" also works at 390.
- **Sunday detail sheets:** 16/16 headed `📅 Sunday's match (4th round)` with opponent button, court, `☀️ Day`/`🌙 Night session`, and time (`11:30 AM` … or "Follows previous match on this court" for m23/m24 where `startTimeET` is null) plus `Match N` tag. Opponent button from Medvedev opens Tiafoe; Escape then closes cleanly. (`http-phone-detail-sunday-alcaraz.png`)
- **Completed matches:** m10 row chips `✅ Done` + `6-2 7-5`, Potapova marked `.won` with 🏆 and aria-label "Open Anastasia Potapova, winner", Anisimova not marked; m14 `✅ Done` + `1-6 7-6(3) 7-5`, Zheng Qinwen winner, Keys not. Detail result lines: Potapova "✅ Won! 6-2 7-5", Zheng "✅ Won! 1-6 7-6(3) 7-5", Anisimova/Keys "💪 Played a great match today!". Grid chips: "🎪 Armstrong · Done", "🎡 Grandstand · Done". (`*-matches-done.png`, `*-detail-done-potapova.png`)
- **In progress:** 4 `🔴 On now` rows (Fritz–Cerúndolo, Blockx–Cobolli, Świątek–Bouzková, Andreeva–Bartůňková), none with a winner mark; all 8 players' sheets say "🔴 On court right now!"; grid chip "🏟️ Ashe · On now". 5 `⏭️ Up next` chips = first unstarted match per court (Ashe, Armstrong, Grandstand, Court 5, Court 17).
- **Sunday block:** `Sunday's 4th-round matches` section with 8 compact rows each showing court + session + `Match N [· time ET]`; hidden whenever a court or session pill is active.
- **Court/session pills:** on "All", each of the 5 court pills and both session pills return >0 cards, 0 Sunday chips, Sunday block hidden. On "Playing Sunday" + a court pill the grid is empty with the labelled empty box (see N1).
- **Tier filter spans both days:** on "All": Superstars 33 (14 Sunday), Seeded 5 (1 Sunday), Underdogs 10 (1 Sunday); on "Playing Sunday" + Superstars → 14, all Sunday. Match list follows the tier filter.
- **Favorites:** star Alcaraz (Sun) + Gauff (Sat), reload → `localStorage nora-favorites` holds both, `aria-pressed=true`, "⭐ 2 collected", 2 ⭐ dots in the match list (one in the Sunday block). ⭐ filter: Sat view 1 card, All 2 cards + 2 matches. Sat view with only a Sunday favorite → empty box, "Show everyone" resets to All 48.
- **Deep link:** fresh tab `#p/carlos-alcaraz` opens the Sunday sheet with photo; ✕ → URL back to `…/us-open-kids-guide/` (file: `…/index.html`), sheet hidden, body unlocked, grid on Sat/32. With prior history, Escape also stays on the page. Both origins.
- **Photos:** 47 JPEGs + Blockx SVG avatar all exist; largest JPEG 28.5KB (all ≤150KB, ≤600px wide); after a full scroll 48/48 grid `<img>` complete with `naturalWidth>0` and alt, 0 fallbacks. Blockx sheet shows "🕵️ No photo yet — a mystery card!" and no photo credit.
- **Credits footer:** 47 `<li>`, each `Name — author — <a>license</a>`, 0 "Unknown"/"see source", Blockx absent. Data check: every non-avatar `photo` has `author`, `license`, `sourceUrl`.
- **Data:** all 52 JSON files parse; `players.js` object == `players.json` minus `factCheck` (48 players / 24 matches); 48 per-player files == merged set; 0 duplicate slugs; every match slug has a card and vice versa; every player's `matchTomorrow.matchId` points at a match on the right day containing both the player and `opponentSlug`; 48/48 `verified`.
- **Paths:** no `src=/`, `href=/`, `url(/` in `index.html`, `app.js`, `sw.js`, `main.css`, `manifest.webmanifest`.
- **Service worker:** `VERSION = 'nora-usopen-v5'`; evaluated inside the worker: `photoList()` = 48, cache `nora-usopen-v5` = **55 entries = 7 shell + 48 `img/players/*`**, all under `/us-open-kids-guide/`, 0 photos missing, all 16 Sunday photos present, old caches gone. Cold offline (one online load, no scroll → offline): `#p/carlos-alcaraz` opens with photo, grid 48/48 images.
- **html-validate 11.13.0:** `index.html` exit 0.

## Blocking (must fix)
- none

## Should fix
- none

## Nice to have
- [N1] `scripts/app.js` `matchesFilters`: with "📅 Playing Sunday" selected, tapping any court/session pill yields "0 of 48 players" + empty box because those pills only describe Saturday's order of play. Consider auto-switching `filters.day` to `'sat'` when a court/session pill is tapped (or disabling those pills on the Sunday view) so a kid never hits a dead end.
- [N2] `data/players/anastasia-potapova.json` `hometown`: sheet reads "Austria · from Vienna, Austria (born in Saratov, Russia)" — `hometownOf()` only strips a trailing ", Austria", so the country is doubled. Change the field to "Vienna (born in Saratov, Russia)" or strip ", <country> (" too.
- [N3] Sunday matches with `startTimeET: null` (m23 Pegula–Cîrstea, m24 Kalinskaya–Navarro) show only "Match 4"/"Match 2" in the compact row; the sheet does show "Follows previous match on this court". Could add the note to the row as well.

## What's great (keep)
- Status chips are consistent across three surfaces (grid chip, match row, detail result line) and the winner is marked with class, 🏆 and aria-label.
- Sunday block is hidden under court/session filters, so the pills never lie about where a player is today.
- Precache picks up the 16 new photos automatically via `players.js`; v5 bump invalidates v2–v4.
- Photo budget: 47 JPEGs total ≈1.0MB, largest 28.5KB.
